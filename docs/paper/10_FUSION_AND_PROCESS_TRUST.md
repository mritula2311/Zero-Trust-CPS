# Fusion and Process Trust

[VERIFIED: configured serving and artifact selection, 2026-09-07, updated after the same-day corrected-M6 promotion] [FusionEngine](../../src/fusion_engine.py) loads `models/fusion_meta_learner.joblib` and `models/fusion_background.npy`. Both are byte-identical to `fusion_meta_learner_m6_corrected_variant.joblib`/`fusion_background_m6_corrected_variant.npy` (13 O2/O5) — the originally-deployed M6 fusion (byte-identical to `fusion_meta_learner_m6_variant.joblib`) is preserved but no longer active. The gateway supplies `[rule_score, isolation_forest_score, lstm_ae_score, gnn_score]`; the last field now carries `SetTransformerScorer` output. It is a compatibility name, not evidence that a GCN produced the value. Security Trust never enters this model.

The four-input sklearn LogisticRegression returns `sigmoid(intercept + Σ coefficient_i * score_i) = P(normal)`. Current coefficients, artifact hashes and historical coefficient lineage are in the single [numerical authority](13_RESULTS_MASTER_TABLES.md). The older [model metadata](../../results/final_verification/model_metadata.json) describes the retained GCN-fitted backup, not today's active fusion. Do not combine that metadata with the current runtime.

[M6 fusion training](../../scripts/train_fusion_meta_learner_m6.py) replays frozen local/M6 scorers on simulated VAL_001, excludes rejected/replay records and normal windows containing earlier anomalies, and fits class-balanced logistic regression. Its background sample comes from that fit matrix. Base TRAIN and meta-training VALIDATION are separate; this is not held-out physical calibration. Serving loads artifacts and performs inference only.

`confidence=max(P(class))` expresses decisiveness, not calibrated uncertainty. Missing model/background files trigger the mean of four process scores with confidence 0.5. Missing relational weights can trigger a neutral score. Transport startup does not prove every learned model is loaded.

## Explainability and policy boundary

The implementation constructs `shap.LinearExplainer(model, background)` and calls `shap_values`. Actual SHAP is therefore computed. For this interventional logistic-linear explanation, background-centered contributions are `w_i*(x_i−E_background[x_i])`; the expected value plus their sum reconstructs the log-odds margin. They are not additive probability changes or causal effects. Isolation Forest Level 2 separately uses actual TreeExplainer; sequence and relational Level 2 use perturbations, which must not be relabeled SHAP.

The largest absolute fusion contribution selects a Level-2 explanation. Current M6 peer attribution removes a peer from the attention key set. Historical GCN peer attribution perturbs graph context. The compatibility field `gnn_score` is retained, while public Level-2 text calls it a relational score. No measurable peer perturbation is not proof that no peers are active.

The gateway stores Process Trust and supplies it alongside separate Security Trust to the configured offline contextual bandit. As of the 2026-09-07 final pass, `scripts/train_adaptive_pdp.py` requires an explicit `RelationalPin` (was: bare `GNNScorer()`/`FusionEngine()` construction reading ambient config, the same mismatch this document flagged) and writes a metadata sidecar attesting the training model/fusion/data hashes; `relational_pin.verify_policy_lineage()` fails loudly on a future mismatch. The deployed Q table (`adaptive_pdp_qtable_m6_corrected.json`) has a matched, verified M6 lineage — see [13](13_RESULTS_MASTER_TABLES.md) O5. Comparative policy quality over a fair GCN baseline is now measured (macro-F1 0.5332 vs. 0.5303, held-out-replay-qualified); physical M6 field trial validation remains open.

## Evidence and reproduction boundary

The preserved GCN-era local, hardware, policy and explainability results remain valid for their recorded protocols. Primary held-out single-channel repair is 79/219; exploratory best-three-channel repair is 179/182 across pooled splits. The latter does not replace the former. See [13](13_RESULTS_MASTER_TABLES.md).

The historical results log reports a GCN-to-M6 fusion improvement with no matching machine-readable comparison receipt; that specific historical number is superseded, not reproduced. A correctly-pinned, machine-readable comparison now exists: `src/relational_pin.py` requires every comparator (`evaluate_ablation_m6.py`, `evaluate_real_hardware.py`, `evaluate_policy_comparison.py`, `evaluate_explainability_level2.py`) to select an explicit `gcn`/`m6_deployed`/`m6_corrected` arm rather than reading ambient config — see `results/comparator_repair/comparator_audit.json` and [13](13_RESULTS_MASTER_TABLES.md) O4. No measured artifact was overwritten in producing it.

Runtime-M6 training's class-weight bug (counting inactive labels before masking the loss, a 7.2x suspicious-class overweight) is FIXED as of 2026-09-07 (`scripts/train_set_transformer.py`) and the corrected checkpoint is now deployed (13 O5). The tick-based-training-vs-wall-clock-serving activity mismatch is also fixed for offline replay: `GNNScorer`/`SetTransformerScorer`/`RuleBasedTrustEngine` now accept an injected deterministic clock (opt-in; live-gateway semantics unchanged) — see `tests/test_policy_training_determinism.py`. There is still no matched local-only versus local-plus-M6 complementarity result, physical M6 field trial or acquisition-to-enforcement latency result. These remain claim-scoped follow-ups in [25](25_OPEN_ITEMS_BEFORE_SUBMISSION.md).
